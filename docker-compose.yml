volumes:
    postgres_data:
        driver: local

services:
    postgres:
        image: postgres:17
        restart: always
        volumes:
            - postgres_data:/var/lib/postgresql/data
        ports:
            - "5557:5432"
        environment:
            POSTGRES_DB: db
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres